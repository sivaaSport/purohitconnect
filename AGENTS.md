# PurohitConnect

One product, one Django backend, one database. Web templates and Flutter (`mobile/`) share `/api/v1/`.

1. Read `.cursor/rules/purohit-core.mdc` and `docs/agent/STATUS.md`.
2. Touch Django → follow `purohit-django.mdc`. Touch Flutter → follow `purohit-flutter.mdc`.
3. Match an existing website flow before inventing API or UX.
4. Implement only the ticket given, then stop. Do not run `docs/DEVELOPMENT_ROADMAP.md` unattended.
5. Commit or push only when asked.

Hats (optional, not separate agents): tests → `agents/testing_agent.md`; live failure → `agents/debugging_agent.md`; large feature design → `agents/feature_agent.md`.

Primary paths: `mobile/`, `apps/api/`, `templates/`. Live status is `docs/agent/STATUS.md`. Historical wallet notes: `agent_handover.md`.
