# Agent handover (historical)

Live status is [`docs/agent/STATUS.md`](docs/agent/STATUS.md). Read that file, not this one, for what is true now.

This file is a pointer only. It used to describe Django wallet, OTP, Razorpay, and booking payment work from before the Flutter devotee app. That snapshot is outdated (no `mobile/`, no `/api/v1/`, no port 8001, no GitHub remote) and is not maintained.

For wallet and payment implementation history, use git log on `apps/accounts/`, `apps/bookings/`, and `apps/accounts/payment_service.py`.
