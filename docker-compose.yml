services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: purohitconnect
      POSTGRES_USER: purohit
      POSTGRES_PASSWORD: purohit
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U purohit -d purohitconnect"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    profiles: ["redis"]

  web:
    build: .
    ports:
      - "${WEB_PORT:-8010}:8000"
    env_file:
      - .env.docker
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.docker
      DATABASE_URL: postgres://purohit:purohit@db:5432/purohitconnect
      REDIS_URL: ${REDIS_URL:-}
      SEED_ON_START: ${SEED_ON_START:-1}
    volumes:
      - media_data:/app/media
      - static_data:/app/staticfiles
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:8000/"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 40s

volumes:
  postgres_data:
  media_data:
  static_data:
